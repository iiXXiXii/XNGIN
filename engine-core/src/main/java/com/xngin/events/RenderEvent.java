package com.xngin.events;

/**
 * Rendering-related events that are fired during render operations.
 * These events allow modules to respond to render state changes, frame boundaries,
 * and render resource lifecycle events. They enable loose coupling between the
 * rendering system and other engine components.
 */
public class RenderEvent extends Event {
}
