package com.xngin.events;

/**
 * Base class for all events in the engine's event system.
 * Events are used to communicate between different parts of the engine
 * without creating tight coupling. Each event carries specific data
 * related to what happened and can be consumed by multiple listeners.
 */
public abstract class Event {
}
