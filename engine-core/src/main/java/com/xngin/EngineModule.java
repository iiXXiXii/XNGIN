package com.xngin;

/**
 * Represents a modular component of the engine that can be initialized, updated, and shut down.
 * Engine modules provide a way to organize different subsystems of the engine (rendering, audio,
 * physics, etc.) into manageable, independent units. Each module has its own lifecycle and can
 * define dependencies on other modules.
 */
public abstract class EngineModule {
}
