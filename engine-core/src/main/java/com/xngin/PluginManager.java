package com.xngin;

/**
 * Manages engine plugins and extensions for modular functionality.
 * This class handles the discovery, loading, and lifecycle management of plugins
 * that extend the engine's capabilities. It supports both internal engine modules
 * and external user-provided plugins, ensuring proper dependency resolution
 * and initialisation order.
 */
public class PluginManager {

}
