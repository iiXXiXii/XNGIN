package com.xngin.assets.loaders;

import com.xngin.assets.Asset;
import com.xngin.assets.Font;

/**
 * Loads font assets from various font file formats.
 * Supports TrueType (TTF), OpenType (OTF), and bitmap font formats.
 * Handles font metadata extraction, glyph atlas generation, and provides
 * support for font families and multiple font weights and styles.
 */
public class FontLoader implements AssetLoader<Font> {
}
