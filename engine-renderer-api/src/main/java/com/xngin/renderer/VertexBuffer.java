package com.xngin.renderer;

/**
 * Represents a vertex buffer for storing mesh vertex data.
 * This class manages vertex buffer objects, providing methods for uploading
 * vertex data, defining vertex attributes, and binding buffers for rendering.
 * It supports both static and dynamic buffer usage patterns and provides
 * efficient memory management for vertex data.
 */
public class VertexBuffer {
}
