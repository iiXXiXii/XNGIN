package com.xngin.renderer;

/**
 * Represents a texture resource for rendering operations.
 * This class encapsulates 1D, 2D, 3D, and cube map textures, providing methods
 * for loading texture data, managing texture parameters (filtering, wrapping),
 * and binding textures to shader uniforms. It supports various texture formats
 * and provides platform-agnostic texture operations.
 */
public class Texture {
}
