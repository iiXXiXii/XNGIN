package com.xngin.scene;

/**
 * Represents a game object in the scene.
 * Entities are the fundamental building blocks of the scene graph and serve as
 * containers for components that define their behavior and properties. Each entity
 * has a unique identifier, a name, and can be part of a parent-child hierarchy.
 * Entities can be created, destroyed, enabled, or disabled during runtime.
 */
public class Entity {
}
