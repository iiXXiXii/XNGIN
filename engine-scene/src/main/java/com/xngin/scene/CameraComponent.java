package com.xngin.scene;

/**
 * Represents a camera in the scene, defining how the 3D world is projected onto a 2D screen.
 * This component stores properties such as field of view, aspect ratio, near and far planes,
 * and projection type (perspective or orthographic). It provides methods for creating view
 * and projection matrices, as well as for ray casting from screen coordinates into the world.
 */
public class CameraComponent extends Component {
}