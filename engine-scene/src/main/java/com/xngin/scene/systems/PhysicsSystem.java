package com.xngin.scene.systems;

import com.xngin.scene.EntitySystem;

/**
 * System responsible for updating and managing physics components.
 * This system processes entities with physics components, updates their
 * physical properties, handles collision responses, and synchronizes
 * physics simulation results with entity transforms. It serves as the
 * bridge between the scene graph and the physics engine.
 */
public class PhysicsSystem extends EntitySystem {
}
