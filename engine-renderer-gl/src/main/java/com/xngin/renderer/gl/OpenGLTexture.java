package com.xngin.renderer.gl;

/**
 * Manages OpenGL texture objects and texture binding.
 * This class handles OpenGL texture creation, parameter setting, data uploading,
 * and binding to texture units. It supports various texture types (2D, 3D, cube maps)
 * and formats while providing efficient texture management and streaming capabilities.
 */
public class OpenGLTexture {
}
